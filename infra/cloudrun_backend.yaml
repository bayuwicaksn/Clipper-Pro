apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: clipper-backend
spec:
  template:
    spec:
      containers:
        - image: BACKEND_IMAGE
          ports:
            - containerPort: 8000
