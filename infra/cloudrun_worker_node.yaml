apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: clipper-worker-node
spec:
  template:
    spec:
      containerConcurrency: 1
      containers:
        - image: WORKER_NODE_IMAGE
          ports:
            - containerPort: 8080
